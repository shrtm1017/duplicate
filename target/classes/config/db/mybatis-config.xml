<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<properties resource="kr/or/ddit/config/db/db.properties"/>
	
	<typeAliases>
		<typeAlias alias="userVo" type="practice.user.model.UserVo"/>
		<typeAlias alias="pageVo" type="practice.util.model.pageVo"/>
		<typeAlias alias="LprodVo" type="practice.lprod.model.LprodVo"/>
		<typeAlias alias="ProdVo" type="practice.prod.model.ProdVo"/>
	
	</typeAliases>
  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${jdbc.driver}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
      </dataSource>
    </environment>
  </environments>
  <mappers>
    <mapper resource="config/db/mapper/user.xml"/>
    <mapper resource="config/db/mapper/lprod.xml"/>
    <mapper resource="config/db/mapper//prod.xml"/>
  </mappers>
</configuration>